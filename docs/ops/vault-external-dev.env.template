# Copy this file to a private, host-local path such as `.env.external-dev`
# before starting the AWS single-node Docker Vault dev server.
#
# Example:
#   cp docs/ops/vault-external-dev.env.template .env.external-dev
#   chmod 600 .env.external-dev
#
# Required:
INTERNAL_SECRET_BOOTSTRAP=replace-me

# Optional overrides. The compose stack already provides the same defaults.
VAULT_ADDR=http://vault-external-dev:8200
VAULT_AUDIT_LOG_PATH=/vault/file/audit.log
VAULT_RUNTIME_ROLE=runtime-core-services
VAULT_ROTATION_ROLE=ops-rotate-internal-secret

GITHUB_OIDC_DISCOVERY_URL=https://token.actions.githubusercontent.com
GITHUB_OIDC_ISSUER=https://token.actions.githubusercontent.com
GITHUB_OIDC_AUDIENCE=https://github.com/DoYouLikeFIx
GITHUB_REPOSITORY_BOUND=DoYouLikeFIx/FIXYZ
GITHUB_JOB_WORKFLOW_REF_GLOB=DoYouLikeFIx/FIXYZ/.github/workflows/docs-publish.yml@*
