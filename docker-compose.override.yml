services:
  mysql:
    ports:
      - "3306:3306"

  redis:
    ports:
      - "6379:6379"

  vault:
    ports:
      - "8200:8200"

  corebank-service:
    ports:
      - "${COREBANK_HOST_PORT:-18081}:8081"

  fep-gateway:
    ports:
      - "${FEP_GATEWAY_HOST_PORT:-18083}:8083"

  fep-simulator:
    ports:
      - "${FEP_SIMULATOR_HOST_PORT:-18082}:8082"
